-- EXPECT-CLEAN
fn clamp<T : Ord> : (T, T, T) -> T
  | (lo, hi, v) ->
    if v < lo then lo
    else if v > hi then hi
    else v

fn abs_val<T : Ord> : T -> T
  | x ->
    if x < 0 then 0 - x else x
