-- Test: early return from loop
fn find_first : (*i64, i64, i64) -> i64
  | (arr, len, target) ->
    let mut i : i64 = 0i64
    while i < len
      if arr[i] == target then return i
      i = i + 1i64
    -1i64
