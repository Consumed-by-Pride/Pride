mod g
=> > ] ∈ ∃ = sizeof interface if x = y static  \ T (
    poison freeze mod handle
-  @ ∈
  block while _ mem 1.5f64
    stage ~
    'c' ¬  opcode graph
    unsafe x pub
  interface for block :
	^ < poison _ += = match and trait + [ ∀
	handle quote
  ... Foo interface <=
  
