mod g
for ^ block => "s"  baz_1 ... < _ as graph splice block  return
  quote baz_1 ^
    effect |> +
	where freeze while "s" & += # == ] handle ; > 0x736f6d6570736575u64 handle |>* %
	assume
  / for  struct ∀ @ >=
    ∃
    ]
_ transmute interface != match or  as not use stage baz_1 <= perform
    [ graph } perform while
	: ; <= ; and effect >= ]
interface 
