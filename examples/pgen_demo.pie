pgen elim_add_zero<T> ->
  [x : T]
  where [x + 0]
  ↦ x

pgen mul_shift<T> ->
  [x : T]
  where [x * 2]
  ↦ x << 1

pgen fma<T> ->
  [a : T, b : T, c : T]
  where [a * b + c]
  ↦ fmaf(a, b, c)

pgen guard_null<T> ->
  [p : T]
  where [p]
  ↦ p
