-- EXPECT: 9809122216071666050
fn fnv1a : (*u8, i64) -> i64
  | (data, len) ->
      let mut hash : i64 = -3750763034362895579
      let mut i : i64 = 0
      while i < len
        hash = hash ^ data[i] as i64
        hash = hash *% 1099511628211
        i = i + 1
      hash

fn print_u64_rt : i64 -> ()
  | n -> ()

fn main : () -> () ! [IO, Alloc]
  | () ->
      let msg : *u8 = ("Hello, Pride!" as ptr) as *u8
      let h = fnv1a(msg, 13)
      print_u64_rt(h)
      syscall(231, 0)
